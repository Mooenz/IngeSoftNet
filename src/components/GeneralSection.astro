---
export interface Props {
	classList?: string;
	idElement?: string;
	style?: string;
}

const { classList, idElement, style } = Astro.props;
---

<section class={`py-10 md:py-16 lg:py-20  ${classList ? `${classList}` : ''}`} id={idElement ? idElement : null} style={style}>
	<slot />
</section>
