---
interface Props {
	large?: boolean;
}

const { large } = Astro.props;
---

<section class={`w-full sm:max-w-xl md:max-w-3xl lg:max-w-7xl mx-auto ${large ? 'xl:max-w-[1600px] px-0' : 'px-4 xl:px-0'}`}>
	<slot />
</section>
